
  # Role-based Restaurant Management System

  This is a code bundle for Role-based Restaurant Management System. The original project is available at https://www.figma.com/design/UTbGAeRgWANZkpiXF5xjmT/Role-based-Restaurant-Management-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  